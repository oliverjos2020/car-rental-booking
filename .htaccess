# <IfModule mod_rewrite.c>
#     RewriteEngine On
    
#     # Special rule for Livewire file preview
#     # RewriteCond %{REQUEST_URI} ^/livewire/preview-file
#     # RewriteRule (.*) public/index.php [L]
 
#     # Route Livewire preview file requests through Laravel
#     RewriteCond %{REQUEST_URI} ^/livewire/preview-file
#     RewriteRule ^ livewire/preview-file [L]

#     # Everything else routes to public
#     RewriteCond %{REQUEST_URI} !^/public/
#     RewriteRule ^(.*)$ public/$1 [L]
    
#     # Websocket rule
#     RewriteCond %{HTTP:Connection} Upgrade [NC]
#     RewriteCond %{HTTP:Upgrade} websocket [NC]
#     RewriteRule ^(.*)$ wss://ws-eu.pusher.com/$1 [P,L]
# </IfModule>

<IfModule mod_rewrite.c>
# That was ONLY to protect you from 500 errors
# if your server did not have mod_rewrite enabled

RewriteEngine On
# RewriteBase /
# NOT needed unless you're using mod_alias to redirect

RewriteCond %{REQUEST_URI} !/public
RewriteRule ^(.*)$ public/$1 [L]
# Direct all requests to /public folder

</IfModule>